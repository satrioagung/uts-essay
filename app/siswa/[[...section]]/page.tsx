"use client";

import { useState } from "react";
import Link from "next/link";
import { useParams, useRouter } from "next/navigation";
import { ArrowRight, Bell, BookOpen, Check, ChevronDown, Clock3, FileText, Home, LogOut, Menu, ShieldCheck, Sparkles, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { cn } from "@/lib/utils";
import { LiveResults, LiveStudentSchedule } from "@/components/student/live-pages";

export default function StudentPage() {
  const params = useParams<{ section?: string[] }>();
  const section = params?.section?.[0] || "jadwal";
  const router = useRouter();
  const [mobileOpen, setMobileOpen] = useState(false);
  const [tokenOpen, setTokenOpen] = useState(false);
  const [selectedSchedule, setSelectedSchedule] = useState("");
  async function startExam(token: string) { const response = await fetch("/api/siswa/sesi", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ jadwalId: selectedSchedule, token }) }); const result = await response.json().catch(() => ({})); if (!response.ok) throw new Error(result.error || "Sesi gagal dimulai."); router.push(`/siswa/ujian/${result.session.id}`); }
  return <div className="min-h-screen bg-canvas"><aside className={cn("fixed inset-y-0 left-0 z-30 flex w-[248px] flex-col border-r border-line bg-white px-4 py-6 transition-transform lg:translate-x-0", mobileOpen ? "translate-x-0" : "-translate-x-full")}><div className="px-2"><div className="flex items-center gap-2.5 text-[17px] font-extrabold tracking-tight text-ink"><div className="grid h-8 w-8 place-items-center rounded-[10px] bg-brand text-white"><Sparkles size={16} /></div>EssaySpace</div></div><div className="mt-11 px-2 text-[10px] font-extrabold uppercase tracking-[.18em] text-[#adb4c2]">Ruang siswa</div><nav className="mt-3 space-y-1">{[{id:"jadwal",label:"Jadwal Ujian",icon:Home},{id:"hasil",label:"Hasil Pengerjaan",icon:FileText}].map(n => { const I=n.icon; return <Link key={n.id} href={`/siswa/${n.id}`} onClick={() => setMobileOpen(false)} className={cn("flex h-10 items-center gap-3 rounded-lg px-3 text-sm font-semibold", section === n.id ? "bg-brand-soft text-brand" : "text-muted hover:bg-canvas hover:text-ink")}><I size={17} />{n.label}</Link>})}</nav><div className="mt-auto"><div className="mb-4 rounded-xl bg-[#fbfbff] p-3"><div className="flex items-center gap-2 text-xs font-bold text-ink"><ShieldCheck size={15} className="text-success" /> Akun terlindungi</div><p className="mt-1 text-[10px] leading-4 text-muted">Jangan bagikan nomor ujianmu.</p></div><div className="flex items-center gap-3 border-t border-line px-2 pt-5"><div className="grid h-9 w-9 place-items-center rounded-full bg-[#dceeff] text-xs font-bold text-[#3574a4]">AF</div><div className="min-w-0 flex-1"><p className="truncate text-xs font-bold text-ink">Ahmad Fauzan</p><p className="text-[11px] text-muted">Siswa</p></div><button onClick={() => router.push("/login")}><LogOut size={15} className="text-muted" /></button></div></div></aside>{mobileOpen && <button aria-label="Tutup menu" onClick={() => setMobileOpen(false)} className="fixed inset-0 z-20 bg-ink/30 lg:hidden" />}<div className="lg:pl-[248px]"><header className="sticky top-0 z-10 flex h-[72px] items-center justify-between border-b border-line bg-white/90 px-5 backdrop-blur-md sm:px-8"><div className="flex items-center gap-3"><button onClick={() => setMobileOpen(true)} className="text-muted lg:hidden"><Menu size={21} /></button><div><p className="text-[11px] font-bold uppercase tracking-[.15em] text-muted">Ruang siswa</p><h1 className="mt-0.5 text-lg font-bold text-ink">Halo 👋</h1></div></div><div className="flex items-center gap-2"><button className="relative grid h-9 w-9 place-items-center rounded-lg text-muted hover:bg-canvas"><Bell size={18} /><span className="absolute right-2 top-2 h-1.5 w-1.5 rounded-full bg-danger" /></button><div className="mx-1 h-5 w-px bg-line" /><div className="hidden items-center gap-2 sm:flex"><div className="grid h-8 w-8 place-items-center rounded-full bg-[#dceeff] text-[10px] font-bold text-[#3574a4]">S</div><span className="text-xs font-bold text-ink">Siswa</span><ChevronDown size={14} className="text-muted" /></div></div></header><main className="mx-auto max-w-[1160px] p-5 sm:p-8">{section === "hasil" ? <LiveResults /> : <LiveStudentSchedule onStart={(id) => { setSelectedSchedule(id); setTokenOpen(true); }} />}</main></div>{tokenOpen && <TokenModal onClose={() => setTokenOpen(false)} onStart={async token => { try { await startExam(token); } catch (error) { throw error; } }} />}</div>;
}

function StudentSchedule({ onStart }: { onStart: () => void }) { return <div className="space-y-7"><div className="rounded-2xl bg-[#24294a] p-6 text-white sm:p-8"><div className="max-w-xl"><p className="text-xs font-bold uppercase tracking-[.18em] text-[#aaaed0]">Ringkasan ujianmu</p><h2 className="mt-3 text-2xl font-extrabold tracking-tight sm:text-3xl">Siapkan dirimu untuk ujian berikutnya.</h2><p className="mt-3 text-sm leading-6 text-[#bdc1d9]">Baca petunjuk dengan teliti dan pastikan koneksi internetmu stabil sebelum memulai.</p><div className="mt-6 flex flex-wrap gap-6 text-xs text-[#c5c8dd]"><span className="flex items-center gap-2"><BookOpen size={15} className="text-[#a19dff]" /> 2 jadwal tersedia</span><span className="flex items-center gap-2"><Clock3 size={15} className="text-[#a19dff]" /> Total 120 menit</span></div></div></div><div><div className="mb-4 flex items-end justify-between"><div><p className="mb-1 text-xs font-bold uppercase tracking-[.15em] text-brand">Jadwal tersedia</p><h2 className="text-xl font-extrabold text-ink">Ujian kamu</h2></div><Badge tone="brand">X TKJ 1</Badge></div><div className="grid gap-4 lg:grid-cols-2"><StudentExamCard active onStart={onStart} subject="Matematika" code="MTK10" date="Hari ini, 24 September 2024" time="08:00 – 10:00" duration="120 menit" questions="24 soal essay" /><StudentExamCard subject="Bahasa Indonesia" code="BIND10" date="Besok, 25 September 2024" time="09:00 – 11:00" duration="120 menit" questions="18 soal essay" /></div></div><Card className="border-[#dcd9ff] bg-[#fbfbff]"><CardContent className="flex gap-3 p-5"><ShieldCheck size={18} className="mt-0.5 shrink-0 text-brand" /><div><p className="text-sm font-bold text-ink">Tips sebelum memulai</p><ul className="mt-2 space-y-1 text-xs leading-5 text-muted"><li>• Pastikan kamu berada di tempat yang tenang.</li><li>• Jawaban tersimpan otomatis setiap kali ada perubahan.</li><li>• Jangan menutup tab atau berpindah aplikasi selama ujian.</li></ul></div></CardContent></Card></div>; }

function StudentExamCard({ active, onStart, subject, code, date, time, duration, questions }: { active?: boolean; onStart?: () => void; subject: string; code: string; date: string; time: string; duration: string; questions: string }) { return <Card className={cn("overflow-hidden", active && "border-brand/30")}><div className={cn("h-1.5", active ? "bg-brand" : "bg-[#e0e3e9]")} /><CardContent className="p-5"><div className="flex items-start justify-between"><div className={cn("grid h-11 w-11 place-items-center rounded-xl text-xs font-extrabold", active ? "bg-brand-soft text-brand" : "bg-canvas text-muted")}>{code.slice(0,3)}</div>{active ? <Badge tone="brand">Hari ini</Badge> : <Badge>Besok</Badge>}</div><h3 className="mt-5 text-lg font-bold text-ink">{subject}</h3><p className="mt-1 text-xs text-muted">Bank soal {code} · X TKJ 1</p><div className="mt-5 grid grid-cols-2 gap-y-3 border-y border-line py-4"><div><p className="text-[10px] uppercase tracking-wider text-muted">Waktu</p><p className="mt-1 text-xs font-bold text-ink">{date}</p><p className="mt-0.5 text-xs text-muted">{time}</p></div><div><p className="text-[10px] uppercase tracking-wider text-muted">Durasi</p><p className="mt-1 text-xs font-bold text-ink">{duration}</p><p className="mt-0.5 text-xs text-muted">{questions}</p></div></div><Button onClick={onStart} disabled={!active} variant={active ? "default" : "secondary"} className="mt-5 w-full">{active ? "Mulai ujian" : "Belum dibuka"}{active && <ArrowRight size={15} />}</Button></CardContent></Card>; }

function TokenModal({ onClose, onStart }: { onClose: () => void; onStart: (token: string) => Promise<void> | void }) { const [token, setToken] = useState(""); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); async function submit() { setError(""); setLoading(true); try { await onStart(token); } catch (reason) { setError(reason instanceof Error ? reason.message : "Sesi gagal dimulai."); setLoading(false); } } return <div className="fixed inset-0 z-50 grid place-items-center bg-ink/40 p-4"><div className="w-full max-w-md rounded-2xl bg-white shadow-2xl"><div className="flex items-start justify-between border-b border-line p-5"><div><div className="mb-3 grid h-10 w-10 place-items-center rounded-xl bg-brand-soft text-brand"><KeyIcon /></div><h2 className="text-lg font-bold text-ink">Masukkan token ujian</h2><p className="mt-1 text-xs leading-5 text-muted">Minta token dari pengawas sebelum melanjutkan.</p></div><button onClick={onClose} className="text-muted"><X size={19} /></button></div><div className="p-5"><label className="block"><span className="mb-2 block text-xs font-bold text-ink">Token ujian</span><input autoFocus value={token} onChange={e => setToken(e.target.value.toUpperCase())} maxLength={6} placeholder="Contoh: A7K2P9" className="h-12 w-full rounded-lg border border-line text-center text-lg font-extrabold tracking-[.35em] outline-none placeholder:text-sm placeholder:font-normal placeholder:tracking-normal focus:border-brand focus:ring-4 focus:ring-brand/10" /></label>{error && <p className="mt-3 rounded-lg bg-danger-soft px-3 py-2 text-xs font-semibold text-danger">{error}</p>}<div className="mt-4 flex gap-2 rounded-xl bg-warning-soft p-3 text-xs leading-5 text-[#956b20]"><ShieldCheck size={15} className="mt-0.5 shrink-0" /> Setelah dimulai, timer akan langsung berjalan dan tidak dapat diulang tanpa persetujuan admin.</div></div><div className="flex justify-end gap-2 border-t border-line p-5"><Button variant="secondary" onClick={onClose}>Batal</Button><Button disabled={token.length < 4 || loading} onClick={() => void submit()}>{loading ? "Memulai..." : "Mulai ujian"} {!loading && <ArrowRight size={15} />}</Button></div></div></div>; }
function KeyIcon() { return <span className="text-sm font-extrabold">KEY</span>; }

function Results() { return <div className="space-y-7"><div><p className="mb-2 text-xs font-bold uppercase tracking-[.15em] text-brand">Riwayat</p><h2 className="text-2xl font-extrabold tracking-tight text-ink">Hasil pengerjaan</h2><p className="mt-1 text-sm text-muted">Lihat status pengerjaan ujianmu.</p></div><Card><CardHeader><CardTitle>Daftar ujian</CardTitle></CardHeader><CardContent className="space-y-3"><ResultRow subject="Matematika" code="MTK10" date="24 September 2024" status="Selesai" /><ResultRow subject="Bahasa Indonesia" code="BIND10" date="Belum dijadwalkan" status="Belum mengerjakan" pending /></CardContent></Card><div className="flex gap-3 rounded-xl border border-[#dcd9ff] bg-[#fbfbff] p-4 text-xs leading-5 text-muted"><ShieldCheck size={17} className="mt-0.5 shrink-0 text-brand" /><span>Nilai essay dinilai manual oleh guru di luar aplikasi. Halaman ini hanya menampilkan status pengerjaan.</span></div></div>; }
function ResultRow({ subject, code, date, status, pending }: { subject: string; code: string; date: string; status: string; pending?: boolean }) { return <div className="flex flex-col gap-3 rounded-xl border border-line p-4 sm:flex-row sm:items-center"><div className={cn("grid h-10 w-10 shrink-0 place-items-center rounded-xl text-xs font-extrabold", pending ? "bg-canvas text-muted" : "bg-success-soft text-success")}>{code.slice(0,3)}</div><div className="flex-1"><p className="text-sm font-bold text-ink">{subject}</p><p className="mt-1 text-xs text-muted">{code} · {date}</p></div><Badge tone={pending ? "neutral" : "success"}>{!pending && <Check size={12} className="mr-1" />}{status}</Badge></div>; }
