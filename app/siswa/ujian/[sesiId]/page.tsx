"use client";

import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { Check, ChevronLeft, ChevronRight, Clock3, Info, Save, Send, ShieldCheck, Sparkles } from "lucide-react";
import { Button } from "@/components/ui/button";
import { ExamRoom } from "@/components/student/exam-room";

const questions = ["Jelaskan pengertian fungsi kuadrat dan berikan satu contoh penerapannya dalam kehidupan sehari-hari.", "Diketahui f(x) = x² - 4x + 3. Tentukan titik puncak dan akar-akar dari fungsi tersebut.", "Sebuah bola dilempar vertikal ke atas dengan kecepatan awal tertentu. Jelaskan hubungan ketinggian bola dengan waktu menggunakan persamaan kuadrat."];

function LegacyExamPage() {
  const router = useRouter();
  const [current, setCurrent] = useState(1);
  const [answer, setAnswer] = useState("");
  const [submitted, setSubmitted] = useState(false);
  const [secondsLeft, setSecondsLeft] = useState(6138);
  useEffect(() => { const interval = window.setInterval(() => setSecondsLeft(value => Math.max(0, value - 1)), 1000); return () => window.clearInterval(interval); }, []);
  useEffect(() => { const timeout = window.setTimeout(() => { void fetch("/api/autosave", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ sesiUjianId: "demo-session", soalId: current, jawabanTeks: answer }) }); }, 500); return () => window.clearTimeout(timeout); }, [answer, current]);
  const timer = `${String(Math.floor(secondsLeft / 3600)).padStart(2, "0")}:${String(Math.floor((secondsLeft % 3600) / 60)).padStart(2, "0")}:${String(secondsLeft % 60).padStart(2, "0")}`;
  if (submitted) return <div className="grid min-h-screen place-items-center bg-canvas p-5"><div className="w-full max-w-md rounded-2xl bg-white p-8 text-center shadow-soft"><div className="mx-auto grid h-14 w-14 place-items-center rounded-full bg-success-soft text-success"><Check size={27} /></div><h1 className="mt-5 text-2xl font-extrabold text-ink">Jawaban terkirim</h1><p className="mt-2 text-sm leading-6 text-muted">Jawabanmu sudah tersimpan. Kamu dapat melihat statusnya di halaman hasil.</p><Button className="mt-6 w-full" onClick={() => router.push("/siswa/hasil")}>Lihat hasil pengerjaan</Button></div></div>;
  return <div className="min-h-screen bg-canvas"><header className="flex h-[68px] items-center justify-between border-b border-line bg-white px-5 sm:px-8"><div className="flex items-center gap-3 text-sm font-extrabold text-ink"><div className="grid h-8 w-8 place-items-center rounded-lg bg-brand text-white"><Sparkles size={15} /></div><span className="hidden sm:inline">EssaySpace</span><span className="text-[#c2c7d1]">/</span><span>Matematika · MTK10</span></div><div className="flex items-center gap-3"><div className="hidden items-center gap-2 rounded-lg bg-success-soft px-3 py-2 text-[11px] font-bold text-success sm:flex"><Save size={13} /> Tersimpan otomatis</div><div className="flex items-center gap-2 rounded-lg bg-danger-soft px-3 py-2 text-xs font-extrabold text-danger"><Clock3 size={14} /> {timer}</div></div></header><div className="mx-auto flex max-w-[1200px] gap-6 p-5 sm:p-8"><aside className="hidden w-56 shrink-0 lg:block"><p className="text-[10px] font-extrabold uppercase tracking-[.17em] text-muted">Daftar soal</p><div className="mt-4 grid grid-cols-4 gap-2">{Array.from({length: 24}, (_, i) => <button key={i} onClick={() => setCurrent(i + 1)} className={`grid h-9 place-items-center rounded-lg text-xs font-bold transition ${current === i + 1 ? "bg-brand text-white" : i < 5 ? "border border-brand/20 bg-brand-soft text-brand" : "border border-line bg-white text-muted hover:border-brand"}`}>{i + 1}</button>)}</div><div className="mt-7 space-y-2 text-[11px] text-muted"><p className="flex items-center gap-2"><span className="h-2 w-2 rounded-full bg-brand" /> Soal aktif</p><p className="flex items-center gap-2"><span className="h-2 w-2 rounded-full bg-brand/30" /> Sudah dijawab</p><p className="flex items-center gap-2"><span className="h-2 w-2 rounded-full bg-[#d9dde5]" /> Belum dijawab</p></div></aside><main className="min-w-0 flex-1"><div className="mb-5 flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[.15em] text-brand">Soal {current} dari 24</p><h1 className="mt-1 text-xl font-extrabold tracking-tight text-ink">Ujian Essay Matematika</h1></div><div className="flex items-center gap-2 text-xs font-semibold text-muted lg:hidden">{current}/24</div></div><div className="rounded-2xl border border-line bg-white p-5 shadow-soft sm:p-8"><div className="flex items-start gap-3"><div className="grid h-8 w-8 shrink-0 place-items-center rounded-lg bg-brand-soft text-xs font-extrabold text-brand">{current}</div><p className="text-base font-bold leading-7 text-ink">{questions[(current - 1) % questions.length]}</p></div><div className="mt-8"><label className="mb-2 block text-xs font-bold text-ink">Jawabanmu</label><textarea value={answer} onChange={e => setAnswer(e.target.value)} rows={11} placeholder="Tulis jawabanmu dengan jelas dan lengkap..." className="w-full resize-none rounded-xl border border-line p-4 text-sm leading-7 outline-none placeholder:text-[#b4bbc7] focus:border-brand focus:ring-4 focus:ring-brand/10" /><div className="mt-2 flex items-center justify-between text-[11px] text-muted"><span>{answer.length} karakter</span><span className="flex items-center gap-1"><Save size={12} /> Autosave aktif</span></div></div></div><div className="mt-5 flex items-center justify-between"><Button variant="secondary" disabled={current === 1} onClick={() => setCurrent(x => x - 1)}><ChevronLeft size={15} /> Sebelumnya</Button>{current < 24 ? <Button onClick={() => setCurrent(x => x + 1)}>Soal berikutnya <ChevronRight size={15} /></Button> : <Button onClick={() => setSubmitted(true)}>Kirim jawaban <Send size={15} /></Button>}</div><div className="mt-6 flex gap-2 rounded-xl bg-warning-soft p-3 text-[11px] leading-5 text-[#956b20]"><Info size={14} className="mt-0.5 shrink-0" /> Jangan menutup tab atau berpindah aplikasi. Pelanggaran akan tercatat dan dapat ditinjau admin.</div></main></div></div>;
}

export default function ExamPage() { return <ExamRoom />; }
