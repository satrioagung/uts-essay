"use client";

import { useEffect, useMemo, useState } from "react";
import { Check, Printer, Search, ShieldCheck } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

type Student = { id: string; nama: string; no_ujian: string; password: string; kelas?: { nama_kelas?: string } };

export function ExamCardsPage() {
  const [students, setStudents] = useState<Student[]>([]); const [selected, setSelected] = useState<string[]>([]); const [search, setSearch] = useState(""); const [header, setHeader] = useState({ line1: "PEMERINTAH PROVINSI JAWA BARAT", line2: "SMK NEGERI 1 CONTOH", line3: "KARTU PESERTA UJIAN" });
  useEffect(() => { fetch("/api/admin/siswa?includeCredentials=true", { cache: "no-store" }).then(r => r.json()).then(result => { const rows = result.data || []; setStudents(rows); setSelected(rows.map((student: Student) => student.id)); }); }, []);
  const filtered = useMemo(() => students.filter(student => `${student.nama} ${student.no_ujian} ${student.kelas?.nama_kelas || ""}`.toLowerCase().includes(search.toLowerCase())), [students, search]);
  const printable = students.filter(student => selected.includes(student.id));
  function toggle(id: string) { setSelected(value => value.includes(id) ? value.filter(item => item !== id) : [...value, id]); }
  return <div className="space-y-6"><div className="print-hidden flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="mb-2 text-xs font-bold uppercase tracking-[.15em] text-brand">Administrasi ujian</p><h2 className="text-2xl font-extrabold tracking-tight text-ink">Cetak Kartu Ujian</h2><p className="mt-1 text-sm text-muted">Atur header kartu dan pilih siswa yang akan dicetak.</p></div><Button onClick={() => window.print()} disabled={!printable.length}><Printer size={16} /> Cetak {printable.length} kartu</Button></div><div className="print-hidden grid gap-5 xl:grid-cols-[.9fr_1.1fr]"><Card><CardHeader><CardTitle>Template header</CardTitle><p className="mt-1 text-xs text-muted">Tiga baris ini akan tampil di bagian atas kartu.</p></CardHeader><CardContent className="space-y-4"><HeaderField label="Header baris 1" value={header.line1} onChange={value => setHeader(previous => ({ ...previous, line1: value }))} /><HeaderField label="Header baris 2" value={header.line2} onChange={value => setHeader(previous => ({ ...previous, line2: value }))} /><HeaderField label="Header baris 3" value={header.line3} onChange={value => setHeader(previous => ({ ...previous, line3: value }))} /><div className="flex gap-2 rounded-xl bg-brand-soft p-3 text-xs leading-5 text-[#5752a9]"><ShieldCheck size={15} className="mt-0.5 shrink-0" /> Header hanya tersimpan selama halaman ini terbuka. Simpan sebagai PDF saat mencetak bila diperlukan.</div></CardContent></Card><Card><CardHeader className="flex flex-row items-center justify-between"><div><CardTitle>Pilih siswa</CardTitle><p className="mt-1 text-xs text-muted">{selected.length} dari {students.length} siswa dipilih</p></div><button onClick={() => setSelected(selected.length === students.length ? [] : students.map(student => student.id))} className="text-xs font-bold text-brand">{selected.length === students.length ? "Batalkan semua" : "Pilih semua"}</button></CardHeader><CardContent><div className="relative mb-3"><Search className="absolute left-3 top-2.5 text-muted" size={15} /><input value={search} onChange={event => setSearch(event.target.value)} placeholder="Cari siswa..." className="h-9 w-full rounded-lg border border-line pl-9 text-xs outline-none focus:border-brand" /></div><div className="max-h-72 space-y-1 overflow-auto">{filtered.map(student => <label key={student.id} className="flex cursor-pointer items-center gap-3 rounded-lg px-2 py-2 hover:bg-canvas"><input type="checkbox" checked={selected.includes(student.id)} onChange={() => toggle(student.id)} className="accent-brand" /><span className="grid h-7 w-7 place-items-center rounded-lg bg-brand-soft text-[10px] font-bold text-brand">{student.nama.slice(0, 2).toUpperCase()}</span><span className="flex-1"><span className="block text-xs font-bold text-ink">{student.nama}</span><span className="block text-[10px] text-muted">{student.no_ujian} · {student.kelas?.nama_kelas || "-"}</span></span></label>)}</div></CardContent></Card></div><div className="print-area space-y-5">{printable.length === 0 ? <Card className="print-hidden"><CardContent className="p-10 text-center text-sm text-muted">Belum ada siswa yang dipilih.</CardContent></Card> : printable.map(student => <ExamCard key={student.id} student={student} header={header} />)}</div></div>;
}

function HeaderField({ label, value, onChange }: { label: string; value: string; onChange: (value: string) => void }) { return <label className="block"><span className="mb-1.5 block text-xs font-bold text-ink">{label}</span><input value={value} onChange={event => onChange(event.target.value)} className="h-10 w-full rounded-lg border border-line px-3 text-sm outline-none focus:border-brand" /></label>; }

function ExamCard({ student, header }: { student: Student; header: { line1: string; line2: string; line3: string } }) { return <div className="exam-card mx-auto w-full max-w-2xl rounded-xl border-2 border-ink bg-white p-6 text-ink shadow-soft"><div className="border-b-2 border-ink pb-4 text-center"><p className="text-sm font-bold uppercase tracking-wide">{header.line1 || "-"}</p><p className="mt-1 text-base font-extrabold uppercase">{header.line2 || "-"}</p><p className="mt-1 text-sm font-bold uppercase tracking-widest">{header.line3 || "-"}</p></div><div className="mt-5 grid grid-cols-[150px_1fr] gap-y-3 text-sm"><span className="font-bold">Nama</span><span>: {student.nama}</span><span className="font-bold">Kelas</span><span>: {student.kelas?.nama_kelas || "-"}</span><span className="font-bold">No peserta ujian</span><span className="font-mono font-bold">: {student.no_ujian}</span><span className="font-bold">Password</span><span className="font-mono font-bold">: {student.password}</span></div><div className="mt-6 flex items-center justify-between border-t border-dashed border-muted pt-3 text-[10px] text-muted"><span>Harap simpan kartu ini dengan baik.</span><span className="flex items-center gap-1"><Check size={12} /> EssaySpace</span></div></div>; }
